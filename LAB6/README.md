# BÁO CÁO BÀI TẬP LAB 6

Kính gửi Thầy Khiêm,

Trong quá trình thực hiện bài tập LAB 6, dự án của em có tích hợp bộ thư viện **OpenCV SDK dành cho Android** file quá nặng vượt mức **1.5GB** để up lên github.

Vì hệ thống GitHub áp dụng quy định nghiêm ngặt, giới hạn dung lượng của mỗi file tải lên không được vượt quá 100 MB (nếu vượt quá sẽ bị hệ thống chặn lại và báo lỗi `Large files detected`), do đó tôi không thể đẩy trực tiếp các file cấu hình nặng này lên kho lưu trữ.

**Ghi chú về mã nguồn:**

- Toàn bộ phần mã nguồn chức năng (code logic, giao diện XML) do em viết trong bài tập này vẫn được đóng gói và tải lên đầy đủ, bảo toàn nguyên vẹn trong thư mục `LAB6`.
- Các file cấu hình quá nặng đã được đưa vào danh sách loại trừ ẩn để quá trình đẩy code lên GitHub không bị gián đoạn.

Em xin lỗi Thầy vì sự bất tiện này và rất mong Thầy thông cảm!
